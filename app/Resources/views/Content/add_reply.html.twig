{% if thread_id is not null %}
    {{ form_start(reply_form) }}
        {{ form_row(reply_form.content) }}
        {{ form_row(reply_form.id_thread, { 'value': thread_id }) }}
    {{ form_end(reply_form) }}
{% endif %}